using UnityEngine;
using UnityEngine.UI;

public class StartManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public Button startButton;
    void Start()
    {
        //need a game start screen on game start
        //the game should not start until the start button is clicked
    }

    // Update is called once per frame
    void Update()
    {
        //if the button is clicked start the game, AND delete the button from the screen.


    }
}
